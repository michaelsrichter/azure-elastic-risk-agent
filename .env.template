# Azure Developer CLI Environment Variables Reference
# NOTE: AZD does NOT read .env files automatically. This file is for reference only.
# Use the following commands to set these values in AZD:

# Azure Configuration (Required)
# azd env new risk-agent-dev
# azd env set AZURE_LOCATION eastus2
# azd env set AZURE_SUBSCRIPTION_ID your-subscription-id-here

# Note: Elasticsearch configuration is handled via Key Vault after deployment
# Azure OpenAI text embedding model is automatically deployed with the infrastructure