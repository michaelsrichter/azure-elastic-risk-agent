{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to .NET Functions",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}",
            "sourceFileMap": {
                "/home/vsts/work/1/s": "${workspaceFolder}"
            }
        }
    ],
    "compounds": []
}
